🎉 Maintenance Operator has been installed!

📝 To enable maintenance mode on an Ingress (using annotations):
   kubectl annotate ingress <ingress-name> {{ .Values.operator.maintenanceAnnotation }}={{ .Values.operator.maintenanceAnnotationValue }}

📝 To enable maintenance mode on a Traefik IngressRoute:
   kubectl annotate ingressroute <ingressroute-name> {{ .Values.operator.maintenanceAnnotation }}={{ .Values.operator.maintenanceAnnotationValue }}

📝 To disable maintenance mode (remove the annotation):
   kubectl annotate ingress <ingress-name> {{ .Values.operator.maintenanceAnnotation }}-
   kubectl annotate ingressroute <ingressroute-name> {{ .Values.operator.maintenanceAnnotation }}-

📝 To use a custom maintenance page (or "default" for default page):
   kubectl annotate ingressroute <ingressroute-name> {{ .Values.operator.customPageAnnotation }}=<page-name>

🔍 Check operator logs:
   kubectl logs -n {{ .Release.Namespace }} deployment/{{ include "maintenance-operator.fullname" . }} -c operator -f

🔍 Check maintenance server logs:
   kubectl logs -n {{ .Release.Namespace }} deployment/{{ include "maintenance-operator.fullname" . }} -c server -f

📊 View backup ConfigMaps:
   kubectl get configmap -n {{ .Release.Namespace }} -l app=maintenance-operator

ℹ️  Maintenance Service: {{ include "maintenance-operator.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

For more information, see the README.md
